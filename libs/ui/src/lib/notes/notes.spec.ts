import { render } from '@testing-library/react';

import { NotesList } from './notes';

describe('NotesList', () => {
  it.skip('should render successfully', () => {
    // const { baseElement } = render(<NotesList />);
    // expect(baseElement).toBeTruthy();
  });
});


